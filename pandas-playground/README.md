# Pandas Playground

This section focuses on **[`pandas-playground`](https://pandas.pydata.org/docs/index.html)**, the Python library for data manipulation and analysis.

### Current Notebooks
- `pandas-join-merge-concat.ipynb` → Learn the differences between `join()`, `merge()`, and `concat()` with simple, practical examples.

### What You’ll Learn
- Index-based vs column-based joins
- SQL-style merges
- Concatenating DataFrames (rows vs columns)
- Handling missing data in joins/concats
